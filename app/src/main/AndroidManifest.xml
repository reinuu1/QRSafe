<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisiuni necesare -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="QRSafe"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.QRSafe"
        tools:replace="android:theme">

        <!-- OnboardingActivity - ecranul de start -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activitatea principala -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Activitatea camerei  ZXing -->
        <activity
            android:name=".ui.scan.CustomCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            tools:replace="android:screenOrientation,android:theme"
            tools:node="replace" />

    </application>

</manifest>
